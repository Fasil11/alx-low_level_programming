#include "main.h"

/**
 * _abs - Computes the absolute value of an intiger
 * @t: The integr to be computed
 * Return: The absoultee value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
